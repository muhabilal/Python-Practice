import numpy as np
a=np.array([20,30,40])
b=np.array([60,70,80])
print(a+b)

c=np.array(["90",80,100])
print(c)
#in the list do not care about how many time digits are present in the list but in the array must have same number of digits in both arrays
d=np.array([[20,30],[20,40]])
print(d)